{
  "name": "coco-butter-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Coco - Chain-abstracted high-yield stablecoin savings platform",
  "scripts": {
    "dev": "pnpm --filter @coco/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec rm -rf node_modules dist out .next"
  },
  "devDependencies": {},
  "pnpm": {
    "overrides": {
      "viem": "2.39.3",
      "@base-org/account": "2.4.0"
    }
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
