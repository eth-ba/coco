{
  "name": "coco-butter-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Coco Butter - Chain-abstracted high-yield stablecoin savings platform",
  "scripts": {
    "dev": "pnpm --filter @coco/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec rm -rf node_modules dist out .next"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
